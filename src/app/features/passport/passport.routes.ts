import { Routes } from '@angular/router';
import { PassportPageComponent } from './pages/passport-page/passport-page.component';

export const PASSPORT_ROUTES: Routes = [{ path: '', component: PassportPageComponent }];
